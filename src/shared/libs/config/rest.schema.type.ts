export type RestSchema = {
  PORT: number;
  SALT: string;
  HOST: string;
  DB_HOST: string;
  DB_PORT: string;
  DB_NAME: string;
  DB_USER: string;
  DB_PASSWORD: string;
  UPLOAD_DIRECTORY: string;
}
